// -*- coding: utf-8, tab-width: 2 -*-

import stub from './stub';

async function translate(ctx) {
  // const { popProp } = ctx;
  // const shouldExist = popProp.mustBe('bool', 'exists');
  return stub(ctx);
}

export default translate;
